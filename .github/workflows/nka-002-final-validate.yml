name: NKA-002 final validate

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/nka-002-final-validate.yml

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    env:
      DATABASE_URL: postgresql://nalka:nalka@127.0.0.1:5432/nalka_ci
      AUTH_SECRET: ci-nalka-auth-route-secret
      NEXTAUTH_SECRET: ci-nalka-auth-route-secret
      NEXTAUTH_URL: http://127.0.0.1:3000
      NEXT_PUBLIC_APP_URL: http://127.0.0.1:3000
      MAIL_MODE: console
      MAIL_FROM: ci@nalka.invalid

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.9
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate clients and route types
        run: |
          pnpm exec prisma generate
          pnpm exec next typegen

      - name: Auth route regression tests
        run: pnpm test:auth-routes

      - name: Typecheck
        run: pnpm exec tsc --noEmit

      - name: Lint
        run: pnpm lint

      - name: Build
        run: pnpm build

      - name: Verify direct event auth redirect
        run: |
          pnpm start > /tmp/nalka-nka002.log 2>&1 &
          for attempt in $(seq 1 30); do
            if curl --silent --output /dev/null http://127.0.0.1:3000/login; then
              break
            fi
            sleep 1
          done

          node <<'NODE'
          async function main() {
            const response = await fetch('http://127.0.0.1:3000/event', { redirect: 'manual' });
            if (![307, 308].includes(response.status)) {
              throw new Error(`Expected 307/308, got ${response.status}`);
            }

            const location = response.headers.get('location');
            if (!location) throw new Error('Missing Location header');

            const target = new URL(location, 'http://127.0.0.1:3000');
            if (target.pathname !== '/login') {
              throw new Error(`Expected canonical /login, got ${location}`);
            }

            console.log(`redirect-ok ${response.status} ${location}`);
          }

          main().catch((error) => {
            console.error(error);
            process.exit(1);
          });
          NODE
